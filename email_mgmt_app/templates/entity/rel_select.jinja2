<div class="col-sm-6"><select class="form-control" name="{{ select_name }}" id="{{ select_id }}"
                              value="{{ select_value }}">
    {{ select_contents }}
</select>
</div>
<div class="col-sm-2">
    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#{{ modal_id }}">
        Create New
    </button>
</div>
